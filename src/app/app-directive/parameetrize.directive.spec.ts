import { ParameetrizeDirective } from './parameetrize.directive';

describe('ParameetrizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ParameetrizeDirective();
    expect(directive).toBeTruthy();
  });
});
